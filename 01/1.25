Should be fine.. Even better because roughly half the number of steps requried..
Apparently if CPU is not string enough to handle large numbers, it moves slower with very large numbers. If it can, it runs faster..
