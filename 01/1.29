(define (simpsons f a b n)
	(define h (/ (- b a) n))
	(define (simpson-term k)
		(define y (f (+ a (* k h))))
		(cond ((or (= k 0) (= k n)) (* 1 y))
			  ((even? k) (* 2 y))
			  (else (* 4 y))))
	(* (sum simpson-term 0 inc n) (/ h 3.0)))
